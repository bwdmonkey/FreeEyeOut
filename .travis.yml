sudo: required
language: python
python:
  - 3.6

services:
  - docker

install:
  - docker-compose up -d scraper

script:
  - python --version
  - pip --version
  - pip list
  - bash scripts/live_tests.sh

after_script:
  - coveralls

notifications:
  email:
    on_sucess: never
    on_failure: always
