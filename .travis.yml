sudo: required
language: python
python:
  - 3.6

services:
  - docker

install:
  - pip install -r requirements.txt
  - docker-compose up -d scraper

script:
  - bash scripts/live_tests.sh

after_script:
  - coveralls
